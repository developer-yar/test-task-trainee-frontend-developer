import styled from "styled-components";

export const AppContainer = styled.div`
  max-width: 75rem;
  padding-inline: 2rem;
  margin: auto;

  @media (max-width: 768px) {
    padding-inline: 1rem;
  }
`;
